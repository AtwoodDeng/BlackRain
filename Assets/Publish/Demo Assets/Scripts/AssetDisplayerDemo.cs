using UnityEngine;
using System.Collections.Generic;

using CF.CameraBot;

public class AssetDisplayerDemo : MonoBehaviour
{
	/*
    public CameraBot CameraBot = null;
	void FixedUpdate ()
    {
        float keyHorizontal = Input.GetAxis("Horizontal");
        float keyVertical = Input.GetAxis("Vertical");
        float mouseHorizontal = Input.GetAxis("Mouse X");
        float mouseVertical = Input.GetAxis("Mouse Y");
        float mouseScrollWheel = Input.GetAxis("Mouse ScrollWheel");

        CameraBot.UpdatePosition(
            keyHorizontal,
            keyVertical,
            mouseHorizontal,
            mouseVertical,
            mouseScrollWheel);
	}
    void OnGUI()
    {
        GUILayout.Label(
            "Esc = Unlock mouse" + "\n" +
            "1 = Stand" + "\n" +
            "2 = LeftBottom" + "\n" +
            "3 = RigbhtBottom" + "\n"+
            "4 = Shoulder");
    }

    void Update()
    {
        if (Input.GetKeyUp(KeyCode.Alpha1))
            CameraBot.SwitchCamera("Stand");
        else if (Input.GetKeyUp(KeyCode.Alpha2))
            CameraBot.SwitchCamera("LeftBottom");
        else if (Input.GetKeyUp(KeyCode.Alpha3))
            CameraBot.SwitchCamera("RigbhtBottom");
        else if (Input.GetKeyUp(KeyCode.Alpha4))
            CameraBot.SwitchCamera("Shoulder");
    }
	*/
}